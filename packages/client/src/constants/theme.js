const theme = {
  spacing: (multiplier = 1) => `${8 * multiplier}px`,
  radius: (multiplier = 1) => `${4 * multiplier}px`,
  fonts: {
    headers: '"Roboto", Sans Serif',
  },
  colors: {
    primary: '#49432a',
    text: {
      light: '#fff',
    },
  },
}

export default theme
